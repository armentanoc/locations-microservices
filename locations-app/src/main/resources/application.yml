server:
  port: 8182

spring:
  application:
    name: LOCATIONS-APP
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

  profiles:
    active: docker
  
  thymeleaf:
    check-template-location: false