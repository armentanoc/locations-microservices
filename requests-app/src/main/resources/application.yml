server:
  port: 8183

spring:
  application:
    name: REQUESTS-APP
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

  profiles:
    active: docker
  
  thymeleaf:
    check-template-location: false
